# Development only. This value is sent as Authorization: Bearer <key> to the local Fastify API.
# Keep this file as a local .env and never commit a real API key.
NUXT_PUBLIC_API_KEY=

# The Vite dev-server proxy forwards /backend/* to this address, avoiding browser CORS.
NUXT_BACKEND_URL=http://127.0.0.1:3000
